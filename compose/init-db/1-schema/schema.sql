-- Cria um schema para agrupar toda a informação relativa aos vinhos.
CREATE SCHEMA IF NOT EXISTS wine;

-- Cria um schema para agrupar as tabelas utilizadas pelo LangChain.
CREATE SCHEMA IF NOT EXISTS langchain;